#include <bits/stdc++.h>

using namespace std;

void round_robin(int time_quantum,vector<int> processes)
{
	int time = 0;
    int process_count = 0;
    while(process_count < processes.size())
    {
        // first check if 
	for(int i=0;i<processes.size();i++)
	{
		if(arrival[i] <= time && processes[i]>0)
		{
			// process is arrived and is not complete
			if(processess[i] 
		}
	}
    }
}


int main()
{
 
}
